/*include information about the standard library. */
#include<stdio.h>

/* Program execution always begins with a special function name main() */
int main()
{
    /* main calls library funtion printf, to print
     * this sequence of character, \n represent new line.  */
    printf("hello, world\n");
    return 0;
}
